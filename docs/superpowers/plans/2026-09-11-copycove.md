# CopyCove 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 一个 macOS 26 菜单栏剪贴板历史工具：⌘; 呼出居中玻璃面板，显示最近 10 条（文本/图片），选中即写回剪贴板并自动 ⌘V 粘贴。

**Architecture:** 纯系统框架（SwiftUI + AppKit + Carbon 热键 + CoreGraphics 事件 + CryptoKit），零第三方依赖。核心逻辑（监听/存储/构建/粘贴）全部写成协议注入的纯类型，可 XCTest 覆盖；UI 层（面板/菜单栏）手动验证。SwiftPM 工程 + 脚本打包 .app（无 Xcode，仅 Command Line Tools）。

**Tech Stack:** Swift 5 语言模式 / SwiftPM / SwiftUI / AppKit (NSPanel, NSStatusItem, NSPasteboard) / Carbon HIToolbox (RegisterEventHotKey) / CGEvent / SMAppService。

**设计文档:** `docs/superpowers/specs/2026-09-11-copycove-design.md`

## Global Constraints

- 部署目标 macOS 26.0（开发机 macOS 26.6.2，仅 Command Line Tools，无 Xcode）
- 零第三方依赖；构建必须只靠 `swift build` / `swift test` / `scripts/build-app.sh`
- 历史上限 10 条（`limit` 常量）；快捷键固定 ⌘;（kVK_ANSI_Semicolon）
- 面板必须 non-activating（不激活 app），否则自动粘贴失效
- 密码隐藏标记 `org.nspasteboard.ConcealedType` 的内容不得记录
- 图片 > 20 MB 跳过（`maxImageBytes` 常量）
- 每个 Task 结束时 `swift build && swift test` 必须全绿再 commit
- 数据目录：`~/Library/Application Support/CopyCove/`（`history.json` + `images/`）

## 文件结构

```
Package.swift                     # SwiftPM 清单
resources/Info.plist              # 打包进 .app 的元数据（LSUIElement 等）
scripts/build-app.sh              # swift build → 组装 CopyCove.app → ad-hoc 签名
Sources/CopyCove/
  main.swift                      # 入口：NSApplication accessory 模式
  AppDelegate.swift               # 装配所有模块、Timer 轮询、权限引导
  ClipItem.swift                  # 数据模型 + CapturedContent + ClipItemBuilder
  RelativeTime.swift              # 相对时间格式化（纯函数）
  Persistence.swift               # PersistenceStore 协议 + DiskStore + InMemoryStore
  HistoryStore.swift              # 内存历史：去重/淘汰/清空
  ClipboardMonitor.swift          # PasteboardProtocol + 轮询监听
  PasteService.swift              # 写回剪贴板 + CGEvent ⌘V（均可注入）
  HotkeyManager.swift             # Carbon RegisterEventHotKey (⌘;)
  HistoryViewModel.swift          # 面板选中状态
  PanelController.swift           # NSPanel 显隐/动画/键盘监听
  StatusBarManager.swift          # 菜单栏图标 + 右键菜单（含开机启动）
  Views/HistoryPanelView.swift    # 面板主视图
  Views/HistoryRowView.swift      # 单行视图
Tests/CopyCoveTests/
  RelativeTimeTests.swift
  ClipItemBuilderTests.swift
  PersistenceTests.swift
  HistoryStoreTests.swift
  ClipboardMonitorTests.swift
  PasteServiceTests.swift
  HistoryViewModelTests.swift
  TestDoubles.swift               # FakePasteboard / InMemoryStore / 图片 fixture
```

---

### Task 1: SwiftPM 脚手架

**Files:**
- Create: `Package.swift`, `Sources/CopyCove/main.swift`, `Tests/CopyCoveTests/SmokeTests.swift`, `.gitignore`

**Interfaces:**
- Produces: 可编译的 `CopyCove` 模块，后续所有文件加入 `Sources/CopyCove/`、测试加入 `Tests/CopyCoveTests/`

- [ ] **Step 1: 写 Package.swift**

```swift
// swift-tools-version:5.10
import PackageDescription

let package = Package(
    name: "CopyCove",
    platforms: [.macOS("26.0")],
    targets: [
        .executableTarget(name: "CopyCove", path: "Sources/CopyCove"),
        .testTarget(name: "CopyCoveTests", dependencies: ["CopyCove"], path: "Tests/CopyCoveTests"),
    ]
)
```

- [ ] **Step 2: 写最小 main.swift**

```swift
import AppKit

let app = NSApplication.shared
let delegate = AppDelegate()
app.delegate = delegate
app.setActivationPolicy(.accessory)
app.run()
```

（`AppDelegate` 在 Task 8 实现；本 Task 先写一个空壳让工程可编译。）

```swift
// Sources/CopyCove/AppDelegate.swift（本 Task 的临时空壳，Task 8 重写）
import AppKit

final class AppDelegate: NSObject, NSApplicationDelegate {}
```

- [ ] **Step 3: 写冒烟测试**

```swift
// Tests/CopyCoveTests/SmokeTests.swift
import XCTest
@testable import CopyCove

final class SmokeTests: XCTestCase {
    func testModuleLoads() { XCTAssertTrue(true) }
}
```

- [ ] **Step 4: 写 .gitignore**

```
.build/
build/
.DS_Store
```

- [ ] **Step 5: 构建验证**

Run: `swift build && swift test`
Expected: BUILD SUCCEEDED；1 个测试通过

- [ ] **Step 6: Commit**

```bash
git add Package.swift Sources Tests .gitignore
git commit -m "chore: SwiftPM 脚手架（macOS 26，零依赖）"
```

---

### Task 2: 数据模型 + 相对时间 + 条目构建器

**Files:**
- Create: `Sources/CopyCove/ClipItem.swift`, `Sources/CopyCove/RelativeTime.swift`
- Test: `Tests/CopyCoveTests/RelativeTimeTests.swift`, `Tests/CopyCoveTests/ClipItemBuilderTests.swift`, `Tests/CopyCoveTests/TestDoubles.swift`

**Interfaces:**
- Consumes: Task 3 将实现的 `PersistenceStore`（本 Task 只引用协议名，写在 `ClipItem.swift` 用到的位置由 Task 3 提供——为避免编译断档，本 Task 在 `TestDoubles.swift` 先落一个最小 `InMemoryStore`，Task 3 把协议正式化并让它 conform）

- [ ] **Step 1: 写失败的测试（TestDoubles + 两个测试文件）**

```swift
// Tests/CopyCoveTests/TestDoubles.swift
import AppKit
@testable import CopyCove

/// 测试用内存"剪贴板"，可编程模拟复制行为
final class FakePasteboard: PasteboardProtocol {
    var changeCount = 0
    var types: [NSPasteboard.PasteboardType]?
    private var storage: [NSPasteboard.PasteboardType: Any] = [:]

    func string(forType type: NSPasteboard.PasteboardType) -> String? { storage[type] as? String }
    func data(forType type: NSPasteboard.PasteboardType) -> Data? { storage[type] as? Data }
    func clearContents() { storage = [:]; types = nil }
    @discardableResult
    func setString(_ string: String, forType type: NSPasteboard.PasteboardType) -> Bool {
        storage[type] = string; return true
    }
    @discardableResult
    func setData(_ data: Data, forType type: NSPasteboard.PasteboardType) -> Bool {
        storage[type] = data; return true
    }

    func simulateCopy(text: String) {
        storage[.string] = text; types = [.string]; changeCount += 1
    }
    func simulateCopy(png: Data) {
        storage[.png] = png; types = [.png]; changeCount += 1
    }
    func simulateConcealedCopy() {
        types = [NSPasteboard.PasteboardType("org.nspasteboard.ConcealedType")]; changeCount += 1
    }
}

/// 测试用内存持久化
final class InMemoryStore: PersistenceStore {
    var items: [ClipItem] = []
    var images: [String: Data] = [:]

    func loadItems() -> [ClipItem] { items }
    func saveItems(_ items: [ClipItem]) { self.items = items }
    func saveImage(data: Data, fileName: String) { images[fileName] = data }
    func imageData(fileName: String) -> Data? { images[fileName] }
    func deleteImage(fileName: String) { images[fileName] = nil }
}

/// 生成 1x1 灰色 PNG 测试图片
enum ImageFixture {
    static func pngData() -> Data {
        let rep = NSBitmapImageRep(bitmapDataPlanes: nil, pixelsWide: 1, pixelsHigh: 1,
                                   bitsPerSample: 8, samplesPerPixel: 4, hasAlpha: true,
                                   isPlanar: false, colorSpaceName: .deviceRGB, bytesPerRow: 0, bitsPerPixel: 0)!
        rep.setColor(NSColor(gray: 0.5, alpha: 1), atX: 0, y: 0)
        return rep.representation(using: .png, properties: [:])!
    }
    static func tiffData() -> Data {
        let image = NSImage(size: NSSize(width: 1, height: 1))
        image.lockFocus()
        NSColor.gray.drawSwatch(in: NSRect(x: 0, y: 0, width: 1, height: 1))
        image.unlockFocus()
        return image.tiffRepresentation!
    }
}
```

```swift
// Tests/CopyCoveTests/RelativeTimeTests.swift
import XCTest
@testable import CopyCove

final class RelativeTimeTests: XCTestCase {
    private var calendar: Calendar {
        var c = Calendar(identifier: .gregorian)
        c.timeZone = TimeZone(identifier: "Asia/Shanghai")!
        return c
    }
    private func date(_ y: Int, _ mo: Int, _ d: Int, _ h: Int, _ mi: Int) -> Date {
        calendar.date(from: DateComponents(year: y, month: mo, day: d, hour: h, minute: mi))!
    }

    func test刚刚() {
        let now = date(2026, 9, 11, 12, 0)
        XCTAssertEqual(RelativeTime.string(from: date(2026, 9, 11, 11, 59), now: now, calendar: calendar), "刚刚")
    }
    func test分钟前() {
        let now = date(2026, 9, 11, 12, 0)
        XCTAssertEqual(RelativeTime.string(from: date(2026, 9, 11, 11, 55), now: now, calendar: calendar), "5 分钟前")
    }
    func test小时前() {
        let now = date(2026, 9, 11, 12, 0)
        XCTAssertEqual(RelativeTime.string(from: date(2026, 9, 11, 9, 0), now: now, calendar: calendar), "3 小时前")
    }
    func test昨天() {
        let now = date(2026, 9, 11, 12, 0)
        XCTAssertEqual(RelativeTime.string(from: date(2026, 9, 10, 23, 0), now: now, calendar: calendar), "昨天")
    }
    func test天前() {
        let now = date(2026, 9, 11, 12, 0)
        XCTAssertEqual(RelativeTime.string(from: date(2026, 9, 8, 12, 0), now: now, calendar: calendar), "3 天前")
    }
    func test超过三十天显示具体日期() {
        let now = date(2026, 9, 11, 12, 0)
        let text = RelativeTime.string(from: date(2026, 7, 1, 12, 0), now: now, calendar: calendar)
        XCTAssertEqual(text, "7月1日")
    }
}
```

```swift
// Tests/CopyCoveTests/ClipItemBuilderTests.swift
import XCTest
@testable import CopyCove

final class ClipItemBuilderTests: XCTestCase {
    func test空文本返回nil() {
        XCTAssertNil(ClipItemBuilder.makeItem(from: .text("   \n "), storage: InMemoryStore()))
        XCTAssertNil(ClipItemBuilder.makeItem(from: .text(""), storage: InMemoryStore()))
    }
    func test文本条目字段完整() {
        let item = ClipItemBuilder.makeItem(from: .text("hello"), storage: InMemoryStore())!
        XCTAssertEqual(item.kind, .text)
        XCTAssertEqual(item.text, "hello")
        XCTAssertNil(item.imageFileName)
        XCTAssertEqual(item.fingerprint, ClipItemBuilder.makeItem(from: .text("hello"), storage: InMemoryStore())!.fingerprint)
    }
    func test图片条目保存PNG文件() {
        let storage = InMemoryStore()
        let item = ClipItemBuilder.makeItem(from: .image(ImageFixture.pngData()), storage: storage)!
        XCTAssertEqual(item.kind, .image)
        let saved = storage.imageData(fileName: item.imageFileName!)!
        XCTAssertEqual(saved.prefix(4), Data([0x89, 0x50, 0x4E, 0x47])) // PNG magic
    }
    func testTIFF被转成PNG() {
        let storage = InMemoryStore()
        let item = ClipItemBuilder.makeItem(from: .image(ImageFixture.tiffData()), storage: storage)!
        let saved = storage.imageData(fileName: item.imageFileName!)!
        XCTAssertEqual(saved.prefix(4), Data([0x89, 0x50, 0x4E, 0x47]))
    }
    func test超大图片跳过() {
        let big = Data(repeating: 0, count: 1024)
        XCTAssertNil(ClipItemBuilder.makeItem(from: .image(big), storage: InMemoryStore(), maxImageBytes: 512))
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `swift test`
Expected: 编译失败（`ClipItem`、`RelativeTime`、`PasteboardProtocol`、`PersistenceStore` 未定义）

- [ ] **Step 3: 实现 ClipItem.swift（模型 + 构建器 + 协议占位）**

```swift
// Sources/CopyCove/ClipItem.swift
import AppKit
import CryptoKit

struct ClipItem: Codable, Identifiable, Equatable {
    enum Kind: String, Codable { case text, image }

    let id: UUID
    let kind: Kind
    let createdAt: Date
    let fingerprint: String
    var text: String?
    var imageFileName: String?
}

/// ClipboardMonitor 捕获到的原始内容（尚未构建成 ClipItem）
enum CapturedContent {
    case text(String)
    case image(Data)
}

enum ClipItemBuilder {
    static func makeItem(from content: CapturedContent,
                         storage: PersistenceStore,
                         maxImageBytes: Int = 20 * 1024 * 1024) -> ClipItem? {
        switch content {
        case .text(let string):
            guard !string.trimmingCharacters(in: .whitespacesAndNewlines).isEmpty else { return nil }
            return ClipItem(id: UUID(), kind: .text, createdAt: Date(),
                            fingerprint: Self.sha256(Data(string.utf8)),
                            text: string, imageFileName: nil)
        case .image(let data):
            let png = Self.normalizeToPNG(data) ?? data
            guard png.count <= maxImageBytes else { return nil }
            let fileName = UUID().uuidString + ".png"
            storage.saveImage(data: png, fileName: fileName)
            return ClipItem(id: UUID(), kind: .image, createdAt: Date(),
                            fingerprint: Self.sha256(png),
                            text: nil, imageFileName: fileName)
        }
    }

    /// TIFF 等格式统一转 PNG；已是 PNG 或无法识别则原样/失败返回
    static func normalizeToPNG(_ data: Data) -> Data? {
        if data.starts(with: [0x89, 0x50, 0x4E, 0x47]) { return data }
        guard let rep = NSBitmapImageRep(data: data) else { return nil }
        return rep.representation(using: .png, properties: [:])
    }

    static func sha256(_ data: Data) -> String {
        SHA256.hash(data: data).map { String(format: "%02x", $0) }.joined()
    }
}
```

- [ ] **Step 4: 实现 RelativeTime.swift**

```swift
// Sources/CopyCove/RelativeTime.swift
import Foundation

enum RelativeTime {
    private static let dayFormatter: DateFormatter = {
        let f = DateFormatter()
        f.dateFormat = "M月d日"
        return f
    }()

    static func string(from date: Date, now: Date = Date(), calendar: Calendar = .current) -> String {
        let comps = calendar.dateComponents([.minute, .hour, .day], from: date, to: now)
        if let day = comps.day, day >= 1 {
            if day == 1 { return "昨天" }
            if day <= 30 { return "\(day) 天前" }
            return dayFormatter.string(from: date)
        }
        if let hour = comps.hour, hour >= 1 { return "\(hour) 小时前" }
        if let minute = comps.minute, minute >= 1 { return "\(minute) 分钟前" }
        return "刚刚"
    }
}
```

- [ ] **Step 5: 运行测试验证通过**

Run: `swift test`
Expected: 全部通过（Smoke 1 + RelativeTime 6 + Builder 5 = 12 个）

- [ ] **Step 6: Commit**

```bash
git add Sources Tests
git commit -m "feat: ClipItem 模型、相对时间格式化、条目构建器（TDD）"
```

---

### Task 3: 持久化层

**Files:**
- Create: `Sources/CopyCove/Persistence.swift`
- Test: `Tests/CopyCoveTests/PersistenceTests.swift`

**Interfaces:**
- Produces: `protocol PersistenceStore: AnyObject { func loadItems() -> [ClipItem]; func saveItems(_: [ClipItem]); func saveImage(data:fileName:); func imageData(fileName:) -> Data?; func deleteImage(fileName:) }`
- `DiskStore(baseURL:)`：baseURL 可注入临时目录用于测试，默认 `~/Library/Application Support/CopyCove`

- [ ] **Step 1: 写失败的测试**

```swift
// Tests/CopyCoveTests/PersistenceTests.swift
import XCTest
@testable import CopyCove

final class PersistenceTests: XCTestCase {
    private var tempDir: URL!
    private var store: DiskStore!

    override func setUp() {
        super.setUp()
        tempDir = FileManager.default.temporaryDirectory
            .appendingPathComponent("CopyCoveTests-\(UUID().uuidString)", isDirectory: true)
        store = DiskStore(baseURL: tempDir)
    }
    override func tearDown() {
        try? FileManager.default.removeItem(at: tempDir)
        super.tearDown()
    }

    private func sampleItem(_ i: Int) -> ClipItem {
        ClipItem(id: UUID(), kind: .text, createdAt: Date(timeIntervalSince1970: TimeInterval(1000 * i)),
                 fingerprint: "fp\(i)", text: "内容\(i)", imageFileName: nil)
    }

    func test条目存取roundtrip() {
        let items = (0..<3).map(sampleItem)
        store.saveItems(items)
        let loaded = DiskStore(baseURL: tempDir).loadItems()
        XCTAssertEqual(loaded.count, 3)
        XCTAssertEqual(loaded.map(\.text), ["内容0", "内容1", "内容2"])
        XCTAssertEqual(loaded.map(\.kind), [.text, .text, .text])
    }
    func test空目录加载返回空() {
        XCTAssertTrue(store.loadItems().isEmpty)
    }
    func test损坏JSON返回空不崩溃() {
        try? Data("not json".utf8).write(to: tempDir.appendingPathComponent("history.json"))
        XCTAssertTrue(store.loadItems().isEmpty)
    }
    func test图片存取与删除() {
        let data = ImageFixture.pngData()
        store.saveImage(data: data, fileName: "a.png")
        XCTAssertEqual(store.imageData(fileName: "a.png"), data)
        store.deleteImage(fileName: "a.png")
        XCTAssertNil(store.imageData(fileName: "a.png"))
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `swift test 2>&1 | tail -5`
Expected: 编译失败（`DiskStore` 未定义）

- [ ] **Step 3: 实现 Persistence.swift**

```swift
// Sources/CopyCove/Persistence.swift
import AppKit

protocol PersistenceStore: AnyObject {
    func loadItems() -> [ClipItem]
    func saveItems(_ items: [ClipItem])
    func saveImage(data: Data, fileName: String)
    func imageData(fileName: String) -> Data?
    func deleteImage(fileName: String)
}

/// 磁盘实现：baseURL 下 history.json + images/ 目录
final class DiskStore: PersistenceStore {
    private let itemsURL: URL
    private let imagesDir: URL
    private let fm = FileManager.default

    init(baseURL: URL? = nil) {
        let base = baseURL ?? Self.defaultBaseURL()
        itemsURL = base.appendingPathComponent("history.json")
        imagesDir = base.appendingPathComponent("images", isDirectory: true)
        try? fm.createDirectory(at: imagesDir, withIntermediateDirectories: true)
    }

    static func defaultBaseURL() -> URL {
        let support = FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask)[0]
        return support.appendingPathComponent("CopyCove", isDirectory: true)
    }

    func loadItems() -> [ClipItem] {
        guard let data = try? Data(contentsOf: itemsURL) else { return [] }
        let decoder = JSONDecoder()
        decoder.dateDecodingStrategy = .iso8601
        return (try? decoder.decode([ClipItem].self, from: data)) ?? []
    }

    func saveItems(_ items: [ClipItem]) {
        let encoder = JSONEncoder()
        encoder.dateEncodingStrategy = .iso8601
        encoder.outputFormatting = [.prettyPrinted, .sortedKeys]
        guard let data = try? encoder.encode(items) else { return }
        try? data.write(to: itemsURL, options: .atomic)
    }

    func saveImage(data: Data, fileName: String) {
        try? data.write(to: imagesDir.appendingPathComponent(fileName), options: .atomic)
    }

    func imageData(fileName: String) -> Data? {
        try? Data(contentsOf: imagesDir.appendingPathComponent(fileName))
    }

    func deleteImage(fileName: String) {
        try? fm.removeItem(at: imagesDir.appendingPathComponent(fileName))
    }

    func loadImage(fileName: String) -> NSImage? {
        NSImage(contentsOf: imagesDir.appendingPathComponent(fileName))
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `swift test`
Expected: 全部通过（新增 4 个）

- [ ] **Step 5: Commit**

```bash
git add Sources Tests
git commit -m "feat: PersistenceStore 协议与 DiskStore 磁盘持久化（TDD）"
```

---

### Task 4: 历史存储（去重/淘汰/清空）

**Files:**
- Create: `Sources/CopyCove/HistoryStore.swift`
- Test: `Tests/CopyCoveTests/HistoryStoreTests.swift`

**Interfaces:**
- Consumes: `PersistenceStore`（Task 3）、`ClipItem`（Task 2）
- Produces: `final class HistoryStore: ObservableObject`：`init(storage: PersistenceStore, limit: Int = 10)`、`@Published private(set) var items: [ClipItem]`、`@discardableResult func add(_ item: ClipItem) -> Bool`、`func removeAll()`

- [ ] **Step 1: 写失败的测试**

```swift
// Tests/CopyCoveTests/HistoryStoreTests.swift
import XCTest
@testable import CopyCove

final class HistoryStoreTests: XCTestCase {
    private func textItem(_ fingerprint: String) -> ClipItem {
        ClipItem(id: UUID(), kind: .text, createdAt: Date(), fingerprint: fingerprint,
                 text: "t-\(fingerprint)", imageFileName: nil)
    }
    private func imageItem(_ fingerprint: String) -> ClipItem {
        ClipItem(id: UUID(), kind: .image, createdAt: Date(), fingerprint: fingerprint,
                 text: nil, imageFileName: fingerprint + ".png")
    }

    func test新增插入最前() {
        let store = HistoryStore(storage: InMemoryStore())
        XCTAssertTrue(store.add(textItem("a")))
        XCTAssertTrue(store.add(textItem("b")))
        XCTAssertEqual(store.items.map(\.fingerprint), ["b", "a"])
    }
    func test与最新一条相同则跳过() {
        let store = HistoryStore(storage: InMemoryStore())
        store.add(textItem("a"))
        XCTAssertFalse(store.add(textItem("a")))
        XCTAssertEqual(store.items.count, 1)
    }
    func test与较旧条目相同不跳过_仅比对最新() {
        let store = HistoryStore(storage: InMemoryStore())
        store.add(textItem("a"))
        store.add(textItem("b"))
        XCTAssertTrue(store.add(textItem("a")))
        XCTAssertEqual(store.items.map(\.fingerprint), ["a", "b", "a"])
    }
    func test超过上限淘汰最旧并删除其图片() {
        let storage = InMemoryStore()
        storage.saveImage(data: Data([1]), fileName: "oldest.png")
        let store = HistoryStore(storage: storage, limit: 2)
        store.add(imageItemWithFile("f1", "oldest.png"))
        store.add(textItem("f2"))
        store.add(textItem("f3")) // 挤掉 f1
        XCTAssertEqual(store.items.map(\.fingerprint), ["f3", "f2"])
        XCTAssertNil(storage.imageData(fileName: "oldest.png"))
    }
    private func imageItemWithFile(_ fingerprint: String, _ fileName: String) -> ClipItem {
        ClipItem(id: UUID(), kind: .image, createdAt: Date(), fingerprint: fingerprint,
                 text: nil, imageFileName: fileName)
    }
    func testinit时从持久化加载() {
        let storage = InMemoryStore()
        storage.saveItems([textItem("x")])
        let store = HistoryStore(storage: storage)
        XCTAssertEqual(store.items.map(\.fingerprint), ["x"])
    }
    func testadd后自动持久化() {
        let storage = InMemoryStore()
        let store = HistoryStore(storage: storage)
        store.add(textItem("a"))
        XCTAssertEqual(storage.items.map(\.fingerprint), ["a"])
    }
    func test清空历史删除全部图片() {
        let storage = InMemoryStore()
        storage.saveImage(data: Data([1]), fileName: "a.png")
        let store = HistoryStore(storage: storage)
        store.add(imageItem("a"))
        store.removeAll()
        XCTAssertTrue(store.items.isEmpty)
        XCTAssertTrue(storage.items.isEmpty)
        XCTAssertNil(storage.imageData(fileName: "a.png"))
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `swift test 2>&1 | tail -5`
Expected: 编译失败（`HistoryStore` 未定义）

- [ ] **Step 3: 实现 HistoryStore.swift**

```swift
// Sources/CopyCove/HistoryStore.swift
import Combine
import Foundation

final class HistoryStore: ObservableObject {
    @Published private(set) var items: [ClipItem]

    private let storage: PersistenceStore
    private let limit: Int

    init(storage: PersistenceStore, limit: Int = 10) {
        self.storage = storage
        self.limit = limit
        self.items = storage.loadItems()
    }

    /// 新条目插到最前；与最新一条相同（kind + fingerprint）则忽略。
    /// 超出上限时淘汰最旧条目并删除其图片文件。
    @discardableResult
    func add(_ item: ClipItem) -> Bool {
        if let newest = items.first, newest.kind == item.kind, newest.fingerprint == item.fingerprint {
            return false
        }
        items.insert(item, at: 0)
        if items.count > limit {
            let evicted = items.removeLast()
            if let fileName = evicted.imageFileName {
                storage.deleteImage(fileName: fileName)
            }
        }
        storage.saveItems(items)
        return true
    }

    func removeAll() {
        for item in items {
            if let fileName = item.imageFileName {
                storage.deleteImage(fileName: fileName)
            }
        }
        items = []
        storage.saveItems(items)
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `swift test`
Expected: 全部通过（新增 7 个）

- [ ] **Step 5: Commit**

```bash
git add Sources Tests
git commit -m "feat: HistoryStore 去重/淘汰/清空与自动持久化（TDD）"
```

---

### Task 5: 剪贴板监听

**Files:**
- Create: `Sources/CopyCove/ClipboardMonitor.swift`
- Test: `Tests/CopyCoveTests/ClipboardMonitorTests.swift`

**Interfaces:**
- Consumes: Task 2 的 `CapturedContent`
- Produces: `protocol PasteboardProtocol: AnyObject`（changeCount/types/string/data/clearContents/setString/setData）；`NSPasteboard` conform 它；`final class ClipboardMonitor`：`init(pasteboard: PasteboardProtocol, maxImageBytes: Int = 20MB)`、`var onCapture: ((CapturedContent) -> Void)?`、`func poll()`、`func noteExternalWrite()`

- [ ] **Step 1: 写失败的测试**

```swift
// Tests/CopyCoveTests/ClipboardMonitorTests.swift
import XCTest
@testable import CopyCove

final class ClipboardMonitorTests: XCTestCase {
    private var pb: FakePasteboard!
    private var monitor: ClipboardMonitor!
    private var captured: [CapturedContent] = []

    override func setUp() {
        super.setUp()
        pb = FakePasteboard()
        monitor = ClipboardMonitor(pasteboard: pb)
        captured = []
        monitor.onCapture = { captured.append($0) }
    }

    func test初始化后无变化不捕获() {
        monitor.poll()
        XCTAssertTrue(captured.isEmpty)
    }
    func test文本变化被捕获() {
        pb.simulateCopy(text: "hello")
        monitor.poll()
        XCTAssertEqual(captured.count, 1)
        if case .text(let s) = captured[0] { XCTAssertEqual(s, "hello") } else { XCTFail() }
    }
    func test同一变化只捕获一次() {
        pb.simulateCopy(text: "hello")
        monitor.poll()
        monitor.poll()
        XCTAssertEqual(captured.count, 1)
    }
    func test密码隐藏标记不捕获() {
        pb.simulateConcealedCopy()
        monitor.poll()
        XCTAssertTrue(captured.isEmpty)
    }
    func test图片在无文本时捕获() {
        pb.simulateCopy(png: ImageFixture.pngData())
        monitor.poll()
        XCTAssertEqual(captured.count, 1)
        if case .image = captured[0] {} else { XCTFail("应为 image") }
    }
    func test文本优先于图片() {
        pb.storageSetBoth(text: "t", png: ImageFixture.pngData())
        monitor.poll()
        if case .text = captured[0] {} else { XCTFail("文本应优先") }
    }
    func test超大图片不捕获() {
        let m = ClipboardMonitor(pasteboard: pb, maxImageBytes: 8)
        m.onCapture = { captured.append($0) }
        pb.simulateCopy(png: Data(repeating: 0, count: 16))
        m.poll()
        XCTAssertTrue(captured.isEmpty)
    }
    func test自己写回的变化被忽略() {
        monitor.noteExternalWrite() // 基线对齐（此时 pb.changeCount 为 0）
        pb.setString("copied-back", forType: .string)
        pb.types = [.string]
        pb.changeCount += 1
        monitor.poll()
        XCTAssertTrue(captured.isEmpty)
    }
}

private extension FakePasteboard {
    func storageSetBoth(text: String, png: Data) {
        setString(text, forType: .string)
        setData(png, forType: .png)
        types = [.string, .png]
        changeCount += 1
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `swift test 2>&1 | tail -5`
Expected: 编译失败（`ClipboardMonitor`、`PasteboardProtocol` 未定义）

- [ ] **Step 3: 实现 ClipboardMonitor.swift**

```swift
// Sources/CopyCove/ClipboardMonitor.swift
import AppKit

/// 对 NSPasteboard 的最小抽象，便于测试注入
protocol PasteboardProtocol: AnyObject {
    var changeCount: Int { get }
    var types: [NSPasteboard.PasteboardType]? { get }
    func string(forType type: NSPasteboard.PasteboardType) -> String?
    func data(forType type: NSPasteboard.PasteboardType) -> Data?
    func clearContents()
    @discardableResult
    func setString(_ string: String, forType type: NSPasteboard.PasteboardType) -> Bool
    @discardableResult
    func setData(_ data: Data, forType type: NSPasteboard.PasteboardType) -> Bool
}

extension NSPasteboard: PasteboardProtocol {}

/// 轮询系统剪贴板，发现新内容通过 onCapture 回调
final class ClipboardMonitor {
    static let concealedType = NSPasteboard.PasteboardType("org.nspasteboard.ConcealedType")

    private let pasteboard: PasteboardProtocol
    private let maxImageBytes: Int
    private var lastChangeCount: Int

    var onCapture: ((CapturedContent) -> Void)?

    init(pasteboard: PasteboardProtocol, maxImageBytes: Int = 20 * 1024 * 1024) {
        self.pasteboard = pasteboard
        self.maxImageBytes = maxImageBytes
        self.lastChangeCount = pasteboard.changeCount
    }

    /// 由外部 Timer 周期调用
    func poll() {
        let count = pasteboard.changeCount
        guard count != lastChangeCount else { return }
        lastChangeCount = count

        guard pasteboard.types?.contains(Self.concealedType) != true else { return }

        if let string = pasteboard.string(forType: .string) {
            onCapture?(.text(string))
        } else if let data = pasteboard.data(forType: .tiff) ?? pasteboard.data(forType: .png) {
            guard data.count <= maxImageBytes else { return }
            onCapture?(.image(data))
        }
    }

    /// CopyCove 自己写回剪贴板后调用，避免把自己的写回当成新复制
    func noteExternalWrite() {
        lastChangeCount = pasteboard.changeCount
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `swift test`
Expected: 全部通过（新增 8 个）

- [ ] **Step 5: Commit**

```bash
git add Sources Tests
git commit -m "feat: ClipboardMonitor 轮询监听（隐藏标记跳过/写回忽略，TDD）"
```

---

### Task 6: 粘贴服务（写回 + 模拟 ⌘V）

**Files:**
- Create: `Sources/CopyCove/PasteService.swift`
- Test: `Tests/CopyCoveTests/PasteServiceTests.swift`

**Interfaces:**
- Consumes: `PasteboardProtocol`、`ClipboardMonitor.noteExternalWrite()`、`PersistenceStore`
- Produces: `final class PasteService`：`init(pasteboard: PasteboardProtocol, monitor: ClipboardMonitor, storage: PersistenceStore, isTrusted: () -> Bool, sendPasteKeystroke: () -> Void, delay: TimeInterval)`、`var onPermissionNeeded: (() -> Void)?`、`func paste(_ item: ClipItem)`

- [ ] **Step 1: 写失败的测试**

```swift
// Tests/CopyCoveTests/PasteServiceTests.swift
import XCTest
@testable import CopyCove

final class PasteServiceTests: XCTestCase {
    private var pb: FakePasteboard!
    private var monitor: ClipboardMonitor!
    private var storage: InMemoryStore!
    private var keystrokeCount = 0
    private var permissionAsked = false

    private func makeService(trusted: Bool) -> PasteService {
        let service = PasteService(
            pasteboard: pb, monitor: monitor, storage: storage,
            isTrusted: { trusted },
            sendPasteKeystroke: { [weak self] in self?.keystrokeCount += 1 },
            delay: 0
        )
        service.onPermissionNeeded = { [weak self] in self?.permissionAsked = true }
        return service
    }

    override func setUp() {
        super.setUp()
        pb = FakePasteboard()
        monitor = ClipboardMonitor(pasteboard: pb)
        storage = InMemoryStore()
        keystrokeCount = 0
        permissionAsked = false
    }

    private func textItem() -> ClipItem {
        ClipItem(id: UUID(), kind: .text, createdAt: Date(), fingerprint: "f",
                 text: "back", imageFileName: nil)
    }
    private func imageItem() -> ClipItem {
        let data = ImageFixture.pngData()
        storage.saveImage(data: data, fileName: "img.png")
        return ClipItem(id: UUID(), kind: .image, createdAt: Date(), fingerprint: "fi",
                        text: nil, imageFileName: "img.png")
    }

    func test粘贴文本写回剪贴板() {
        makeService(trusted: true).paste(textItem())
        XCTAssertEqual(pb.string(forType: .string), "back")
    }
    func test粘贴图片写回PNG数据() {
        makeService(trusted: true).paste(imageItem())
        XCTAssertEqual(pb.data(forType: .png), ImageFixture.pngData())
    }
    func test写回后轮询不会误捕获() {
        let captured = CapturedRecorder()
        monitor.onCapture = { captured.items.append($0) }
        makeService(trusted: true).paste(textItem())
        monitor.poll()
        XCTAssertTrue(captured.items.isEmpty)
    }
    func test无权限时不发按键并回调() {
        makeService(trusted: false).paste(textItem())
        XCTAssertEqual(pb.string(forType: .string), "back") // 已写回
        XCTAssertEqual(keystrokeCount, 0)
        XCTAssertTrue(permissionAsked)
    }
    func test有权限时异步发送按键() {
        let exp = expectation(description: "keystroke sent")
        keystrokeSent = exp
        makeService(trusted: true).paste(textItem())
        wait(for: [exp], timeout: 1)
        XCTAssertEqual(keystrokeCount, 1)
    }
    private var keystrokeSent: XCTestExpectation?
}

private final class CapturedRecorder {
    var items: [CapturedContent] = []
}
```

（`keystrokeSent` 需在闭包里 fulfill，把 `makeService` 里 `sendPasteKeystroke` 闭包改为同时触发 `self?.keystrokeSent?.fulfill()`——实现测试时按此微调：`sendPasteKeystroke: { [weak self] in self?.keystrokeCount += 1; self?.keystrokeSent?.fulfill() }`。）

- [ ] **Step 2: 运行测试验证失败**

Run: `swift test 2>&1 | tail -5`
Expected: 编译失败（`PasteService` 未定义）

- [ ] **Step 3: 实现 PasteService.swift**

```swift
// Sources/CopyCove/PasteService.swift
import AppKit
import ApplicationServices

/// 把历史条目写回剪贴板，并在有辅助功能权限时模拟 ⌘V
final class PasteService {
    private let pasteboard: PasteboardProtocol
    private let monitor: ClipboardMonitor
    private let storage: PersistenceStore
    private let isTrusted: () -> Bool
    private let sendPasteKeystroke: () -> Void
    private let delay: TimeInterval

    /// 无辅助功能权限时触发（用于提示用户）
    var onPermissionNeeded: (() -> Void)?

    init(pasteboard: PasteboardProtocol,
         monitor: ClipboardMonitor,
         storage: PersistenceStore,
         isTrusted: @escaping () -> Bool = { AXIsProcessTrusted() },
         sendPasteKeystroke: @escaping () -> Void = PasteService.sendCmdV,
         delay: TimeInterval = 0.1) {
        self.pasteboard = pasteboard
        self.monitor = monitor
        self.storage = storage
        self.isTrusted = isTrusted
        self.sendPasteKeystroke = sendPasteKeystroke
        self.delay = delay
    }

    func paste(_ item: ClipItem) {
        writeToPasteboard(item)
        monitor.noteExternalWrite()

        guard isTrusted() else {
            onPermissionNeeded?()
            return
        }
        // 等面板收起、系统把键盘焦点还给原应用，再模拟 ⌘V
        DispatchQueue.main.asyncAfter(deadline: .now() + delay) { [sendPasteKeystroke] in
            sendPasteKeystroke()
        }
    }

    private func writeToPasteboard(_ item: ClipItem) {
        pasteboard.clearContents()
        switch item.kind {
        case .text:
            pasteboard.setString(item.text ?? "", forType: .string)
        case .image:
            if let fileName = item.imageFileName,
               let data = storage.imageData(fileName: fileName) {
                pasteboard.setData(data, forType: .png)
            }
        }
    }

    /// 模拟按下并释放 ⌘V（需要辅助功能权限）
    static func sendCmdV() {
        guard let source = CGEventSource(stateID: .combinedSessionState) else { return }
        let vKey: CGKeyCode = 9 // kVK_ANSI_V
        let flags = CGEventFlags.maskCommand
        let down = CGEvent(keyboardEventSource: source, virtualKey: vKey, keyDown: true)
        let up = CGEvent(keyboardEventSource: source, virtualKey: vKey, keyDown: false)
        down?.flags = flags
        up?.flags = flags
        down?.post(tap: .cghidEventTap)
        up?.post(tap: .cghidEventTap)
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `swift test`
Expected: 全部通过（新增 5 个）

- [ ] **Step 5: Commit**

```bash
git add Sources Tests
git commit -m "feat: PasteService 写回剪贴板与模拟 ⌘V（权限降级，TDD）"
```

---

### Task 7: 面板 ViewModel

**Files:**
- Create: `Sources/CopyCove/HistoryViewModel.swift`
- Test: `Tests/CopyCoveTests/HistoryViewModelTests.swift`

**Interfaces:**
- Consumes: `HistoryStore`（Task 4）
- Produces: `final class HistoryViewModel: ObservableObject`：`@Published private(set) var items: [ClipItem]`、`@Published var selectedIndex: Int`、`init(store: HistoryStore)`、`func moveSelection(_ delta: Int)`（循环滚动）

- [ ] **Step 1: 写失败的测试**

```swift
// Tests/CopyCoveTests/HistoryViewModelTests.swift
import XCTest
@testable import CopyCove

final class HistoryViewModelTests: XCTestCase {
    private func makeStore(count: Int) -> HistoryStore {
        let storage = InMemoryStore()
        let items = (0..<count).map { i in
            ClipItem(id: UUID(), kind: .text, createdAt: Date(), fingerprint: "f\(i)",
                     text: "t\(i)", imageFileName: nil)
        }
        storage.saveItems(items.reversed()) // 最新在前
        return HistoryStore(storage: storage)
    }

    func test初始选中第一条() {
        XCTAssertEqual(HistoryViewModel(store: makeStore(count: 3)).selectedIndex, 0)
    }
    func test向下移动() {
        let vm = HistoryViewModel(store: makeStore(count: 3))
        vm.moveSelection(1)
        XCTAssertEqual(vm.selectedIndex, 1)
    }
    func test到底后循环回第一条() {
        let vm = HistoryViewModel(store: makeStore(count: 3))
        vm.moveSelection(1); vm.moveSelection(1); vm.moveSelection(1)
        XCTAssertEqual(vm.selectedIndex, 0)
    }
    func test在第一条向上移动循环到最后一条() {
        let vm = HistoryViewModel(store: makeStore(count: 3))
        vm.moveSelection(-1)
        XCTAssertEqual(vm.selectedIndex, 2)
    }
    func test列表更新后越界选中复位() {
        let storage = InMemoryStore()
        let store = HistoryStore(storage: storage)
        let vm = HistoryViewModel(store: store)
        store.add(ClipItem(id: UUID(), kind: .text, createdAt: Date(), fingerprint: "1", text: "a", imageFileName: nil))
        store.add(ClipItem(id: UUID(), kind: .text, createdAt: Date(), fingerprint: "2", text: "b", imageFileName: nil))
        vm.moveSelection(1)
        XCTAssertEqual(vm.selectedIndex, 1)
        store.removeAll() // 触发 items 更新
        XCTAssertEqual(vm.selectedIndex, 0)
    }
}
```

- [ ] **Step 2: 运行测试验证失败**

Run: `swift test 2>&1 | tail -5`
Expected: 编译失败（`HistoryViewModel` 未定义）

- [ ] **Step 3: 实现 HistoryViewModel.swift**

```swift
// Sources/CopyCove/HistoryViewModel.swift
import Combine
import Foundation

final class HistoryViewModel: ObservableObject {
    @Published private(set) var items: [ClipItem]
    @Published var selectedIndex: Int = 0

    private var cancellable: AnyCancellable?

    init(store: HistoryStore) {
        items = store.items
        cancellable = store.$items
            .receive(on: DispatchQueue.main)
            .sink { [weak self] newItems in
                guard let self else { return }
                self.items = newItems
                if self.selectedIndex >= newItems.count {
                    self.selectedIndex = 0
                }
            }
    }

    /// 上/下移动选择，列表两端循环
    func moveSelection(_ delta: Int) {
        guard !items.isEmpty else { return }
        selectedIndex = (selectedIndex + delta + items.count) % items.count
    }
}
```

- [ ] **Step 4: 运行测试验证通过**

Run: `swift test`
Expected: 全部通过（新增 5 个）

- [ ] **Step 5: Commit**

```bash
git add Sources Tests
git commit -m "feat: HistoryViewModel 选中状态与循环移动（TDD）"
```

---

### Task 8: 热键 + 菜单栏 + App 装配

**Files:**
- Create: `Sources/CopyCove/HotkeyManager.swift`, `Sources/CopyCove/StatusBarManager.swift`
- Modify: `Sources/CopyCove/AppDelegate.swift`（重写 Task 1 的空壳）

**Interfaces:**
- Consumes: 前面所有核心模块
- Produces: `final class HotkeyManager`：`init(keyCode: UInt32 = UInt32(kVK_ANSI_Semicolon), modifiers: UInt32 = UInt32(cmdKey), handler: @escaping () -> Void)`、`func start() -> Bool`；`final class StatusBarManager`：`init(onClear: @escaping () -> Void, onQuit: @escaping () -> Void)`、`func install()`
- 本 Task 的 UI 部分无法单测，用编译 + 运行冒烟验证

- [ ] **Step 1: 实现 HotkeyManager.swift**

```swift
// Sources/CopyCove/HotkeyManager.swift
import Carbon.HIToolbox
import Foundation

/// 全局热键（Carbon RegisterEventHotKey，系统自带，无第三方依赖）
final class HotkeyManager {
    private let keyCode: UInt32
    private let modifiers: UInt32
    private let handler: () -> Void
    private var hotKeyRef: EventHotKeyRef?
    private var eventHandler: EventHandlerRef?

    init(keyCode: UInt32 = UInt32(kVK_ANSI_Semicolon), // ⌘;
         modifiers: UInt32 = UInt32(cmdKey),
         handler: @escaping () -> Void) {
        self.keyCode = keyCode
        self.modifiers = modifiers
        self.handler = handler
    }

    func start() -> Bool {
        var eventSpec = EventTypeSpec(eventClass: OSType(kEventClassKeyboard),
                                      eventKind: UInt32(kEventHotKeyPressed))
        let callback: EventHandlerUPP = { _, event, userData in
            // 热键 ID 一致才触发（本应用只注册一个热键，直接回调即可）
            let manager = Unmanaged<HotkeyManager>.fromOpaque(userData!).takeUnretainedValue()
            manager.handler()
            return noErr
        }
        let installStatus = InstallEventHandler(GetApplicationEventTarget(), callback, 1,
                                                &eventSpec,
                                                Unmanaged.passUnretained(self).toOpaque(),
                                                &eventHandler)
        guard installStatus == noErr else { return false }

        let hotKeyID = EventHotKeyID(signature: OSType(0x4343_4356), id: 1) // 'CCCV'
        var ref: EventHotKeyRef?
        let registerStatus = RegisterEventHotKey(keyCode, modifiers, hotKeyID,
                                                 GetApplicationEventTarget(), 0, &ref)
        hotKeyRef = ref
        return registerStatus == noErr
    }
}
```

- [ ] **Step 2: 实现 StatusBarManager.swift**

```swift
// Sources/CopyCove/StatusBarManager.swift
import AppKit
import ServiceManagement

final class StatusBarManager {
    private var statusItem: NSStatusItem?

    private let onClear: () -> Void
    private let onQuit: () -> Void

    init(onClear: @escaping () -> Void, onQuit: @escaping () -> Void) {
        self.onClear = onClear
        self.onQuit = onQuit
    }

    func install() {
        let item = NSStatusBar.system.statusItem(withLength: NSStatusItem.squareLength)
        item.button?.image = NSImage(systemSymbolName: "doc.on.clipboard",
                                     accessibilityDescription: "CopyCove")
        let menu = NSMenu()

        let clearItem = NSMenuItem(title: "清空历史", action: nil, keyEquivalent: "")
        clearItem.target = self
        clearItem.action = #selector(clearHistory)
        menu.addItem(clearItem)

        let launchItem = NSMenuItem(title: "开机启动", action: nil, keyEquivalent: "")
        launchItem.target = self
        launchItem.action = #selector(toggleLaunchAtLogin)
        launchItem.state = SMAppService.mainApp.status == .enabled ? .on : .off
        menu.addItem(launchItem)

        menu.addItem(.separator())
        let quitItem = NSMenuItem(title: "退出 CopyCove", action: nil, keyEquivalent: "q")
        quitItem.target = self
        quitItem.action = #selector(quit)
        menu.addItem(quitItem)

        item.menu = menu
        statusItem = item
    }

    @objc private func clearHistory() { onClear() }
    @objc private func quit() { onQuit() }

    @objc private func toggleLaunchAtLogin(_ sender: NSMenuItem) {
        do {
            switch SMAppService.mainApp.status {
            case .enabled:
                try SMAppService.mainApp.unregister()
                sender.state = .off
            default:
                try SMAppService.mainApp.register()
                sender.state = .on
            }
        } catch {
            sender.state = .off
            let alert = NSAlert()
            alert.messageText = "无法设置开机启动"
            alert.informativeText = "请把 CopyCove.app 放到「应用程序」文件夹后再试。\n(\(error.localizedDescription))"
            alert.runModal()
        }
    }
}
```

- [ ] **Step 3: 重写 AppDelegate.swift**

```swift
// Sources/CopyCove/AppDelegate.swift
import AppKit
import ApplicationServices

final class AppDelegate: NSObject, NSApplicationDelegate {
    private let storage = DiskStore()
    private lazy var store = HistoryStore(storage: storage)
    private lazy var monitor = ClipboardMonitor(pasteboard: NSPasteboard.general)
    private lazy var pasteService = PasteService(pasteboard: NSPasteboard.general,
                                                 monitor: monitor,
                                                 storage: storage)
    private lazy var viewModel = HistoryViewModel(store: store)
    private lazy var panelController = PanelController(viewModel: viewModel,
                                                       imageProvider: { fileName in
                                                           storage.loadImage(fileName: fileName)
                                                       })
    private var hotkey: HotkeyManager?
    private var statusBar: StatusBarManager?
    private var timer: Timer?

    func applicationDidFinishLaunching(_ notification: Notification) {
        monitor.onCapture = { [weak self] content in
            guard let self else { return }
            if let item = ClipItemBuilder.makeItem(from: content, storage: self.storage) {
                self.store.add(item)
            }
        }
        timer = Timer.scheduledTimer(withTimeInterval: 0.5, repeats: true) { [weak self] _ in
            self?.monitor.poll()
        }

        panelController.onPaste = { [weak self] item in
            guard let self else { return }
            self.panelController.hide()
            self.pasteService.paste(item)
        }
        pasteService.onPermissionNeeded = { [weak self] in
            self?.showAccessibilityGuide()
        }

        hotkey = HotkeyManager { [weak self] in self?.panelController.toggle() }
        if hotkey?.start() != true {
            NSLog("CopyCove: ⌘; 热键注册失败（可能被其他应用占用）")
        }

        statusBar = StatusBarManager(onClear: { [weak self] in self?.store.removeAll() },
                                     onQuit: { NSApp.terminate(nil) })
        statusBar?.install()

        if !AXIsProcessTrusted() {
            showAccessibilityGuide()
        }
    }

    /// 辅助功能权限引导（无权限则每次启动提示一次）
    private func showAccessibilityGuide() {
        let alert = NSAlert()
        alert.alertStyle = .informational
        alert.messageText = "启用「选中即粘贴」"
        alert.informativeText = """
        CopyCove 需要辅助功能权限，才能在你选中历史条目后自动粘贴到光标处。
        不授权也可以使用：条目仍会写回剪贴板，你手动按 ⌘V 即可。
        """
        alert.addButton(withTitle: "打开系统设置")
        alert.addButton(withTitle: "稍后")
        if alert.runModal() == .alertFirstButtonReturn {
            let url = URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_Accessibility")!
            NSWorkspace.shared.open(url)
        }
    }
}
```

- [ ] **Step 4: 编译验证**

Run: `swift build 2>&1 | tail -5`
Expected: BUILD SUCCEEDED（PanelController 尚未实现会报错——本 Task 先创建最小占位 `PanelController`，Task 9 完整实现。占位如下）

```swift
// Sources/CopyCove/PanelController.swift（本 Task 临时占位，Task 9 重写）
import AppKit

final class PanelController {
    var onPaste: ((ClipItem) -> Void)?
    init(viewModel: HistoryViewModel, imageProvider: @escaping (String) -> NSImage?) {}
    func toggle() {}
    func hide() {}
}
```

- [ ] **Step 5: Commit**

```bash
git add Sources
git commit -m "feat: ⌘; 全局热键、菜单栏（含开机启动）、AppDelegate 装配"
```

---

### Task 9: 面板 UI + 打包脚本 + 手动验收

**Files:**
- Create: `Sources/CopyCove/Views/HistoryPanelView.swift`, `Sources/CopyCove/Views/HistoryRowView.swift`, `resources/Info.plist`, `scripts/build-app.sh`, `README.md`
- Modify: `Sources/CopyCove/PanelController.swift`（完整重写占位实现）

**Interfaces:**
- Consumes: `HistoryViewModel`、`RelativeTime`、`ClipItem`
- Produces: 完整可运行的 CopyCove.app

- [ ] **Step 1: 实现 Views/HistoryRowView.swift**

```swift
// Sources/CopyCove/Views/HistoryRowView.swift
import AppKit
import SwiftUI

struct HistoryRowView: View {
    let item: ClipItem
    let index: Int
    let isSelected: Bool
    let imageProvider: (String) -> NSImage?

    @State private var hovered = false

    private var hotkeyBadge: String { index == 9 ? "0" : String(index + 1) }

    var body: some View {
        HStack(spacing: 10) {
            Image(systemName: item.kind == .text ? "doc.plaintext" : "photo")
                .font(.system(size: 13))
                .foregroundStyle(.secondary)
                .frame(width: 16)

            if item.kind == .text {
                Text(verbatim: item.text ?? "")
                    .font(.system(size: 13))
                    .foregroundStyle(.primary)
                    .lineLimit(2)
            } else if let image = item.imageFileName.map(imageProvider) {
                Image(nsImage: image)
                    .resizable()
                    .scaledToFit()
                    .frame(width: 26, height: 26)
                    .clipShape(RoundedRectangle(cornerRadius: 4, style: .continuous))
            }

            Spacer(minLength: 8)

            Text(hotkeyBadge)
                .font(.system(size: 11, design: .monospaced))
                .foregroundStyle(.quaternary)

            Text(RelativeTime.string(from: item.createdAt))
                .font(.system(size: 11))
                .foregroundStyle(.tertiary)
        }
        .padding(.horizontal, 10)
        .padding(.vertical, 8)
        .frame(maxWidth: .infinity, minHeight: 30, alignment: .leading)
        .background {
            if isSelected || hovered {
                RoundedRectangle(cornerRadius: 8, style: .continuous)
                    .fill(isSelected ? Color.quaternary.opacity(0.9) : Color.quaternary.opacity(0.4))
            }
        }
        .onHover { hovered = $0 }
    }
}
```

- [ ] **Step 2: 实现 Views/HistoryPanelView.swift**

```swift
// Sources/CopyCove/Views/HistoryPanelView.swift
import AppKit
import SwiftUI

struct HistoryPanelView: View {
    @ObservedObject var viewModel: HistoryViewModel
    let imageProvider: (String) -> NSImage?
    let onPasteItem: (ClipItem) -> Void

    var body: some View {
        Group {
            if viewModel.items.isEmpty {
                VStack(spacing: 8) {
                    Image(systemName: "tray")
                        .font(.system(size: 24))
                        .foregroundStyle(.quaternary)
                    Text("暂无剪贴板历史")
                        .font(.system(size: 13))
                        .foregroundStyle(.secondary)
                }
                .frame(maxWidth: .infinity, minHeight: 120)
            } else {
                VStack(spacing: 2) {
                    ForEach(Array(viewModel.items.enumerated()), id: \.element.id) { index, item in
                        HistoryRowView(item: item, index: index,
                                       isSelected: index == viewModel.selectedIndex,
                                       imageProvider: imageProvider)
                            .contentShape(Rectangle())
                            .onTapGesture { onPasteItem(item) }
                    }
                }
                .padding(8)
            }
        }
        .frame(width: 420)
        .background(.ultraThinMaterial,
                    in: RoundedRectangle(cornerRadius: 16, style: .continuous))
        .overlay(
            RoundedRectangle(cornerRadius: 16, style: .continuous)
                .strokeBorder(.separator, lineWidth: 1)
        )
    }
}
```

- [ ] **Step 3: 完整重写 PanelController.swift**

```swift
// Sources/CopyCove/PanelController.swift
import AppKit
import Carbon.HIToolbox
import SwiftUI

/// 允许 borderless 面板成为 key window（接收键盘）而不激活应用
final class CopyCovePanel: NSPanel {
    override var canBecomeKey: Bool { true }
}

final class PanelController: NSObject {
    var onPaste: ((ClipItem) -> Void)?

    private let viewModel: HistoryViewModel
    private let imageProvider: (String) -> NSImage?
    private var panel: CopyCovePanel?
    private var hostingView: NSHostingView<HistoryPanelView>?
    private var keyMonitor: Any?
    private var resignKeyObserver: NSObjectProtocol?

    private let panelWidth: CGFloat = 420

    init(viewModel: HistoryViewModel, imageProvider: @escaping (String) -> NSImage?) {
        self.viewModel = viewModel
        self.imageProvider = imageProvider
        super.init()
        resignKeyObserver = NotificationCenter.addObserver(
            forName: NSWindow.didResignKeyNotification, object: nil, queue: .main
        ) { [weak self] note in
            guard let self, note.object === self.panel else { return }
            self.hide(animated: false)
        }
    }

    var isVisible: Bool { panel?.isVisible ?? false }

    func toggle() {
        if isVisible { hide() } else { show() }
    }

    func show() {
        let panel = obtainPanel()
        panel.setContentSize(hostingView!.fittingSize)
        position(panel)
        viewModel.selectedIndex = 0

        panel.alphaValue = 0
        panel.makeKeyAndOrderFront(nil)
        let animation = NSAnimationContext.runAnimationGroup
        animation({ context in
            context.duration = 0.15
            context.timingFunction = CAMediaTimingFunction(name: .easeOut)
            panel.animator().alphaValue = 1
        })
        popScaleAnimation(on: panel)

        installKeyMonitorIfNeeded()
    }

    func hide() { hide(animated: true) }

    func hide(animated: Bool) {
        guard let panel, panel.isVisible else { return }
        if animated {
            NSAnimationContext.runAnimationGroup({ context in
                context.duration = 0.1
                panel.animator().alphaValue = 0
            }, completionHandler: {
                panel.orderOut(nil)
                panel.alphaValue = 1
            })
        } else {
            panel.orderOut(nil)
            panel.alphaValue = 1
        }
    }

    // MARK: - Private

    private func obtainPanel() -> CopyCovePanel {
        if let panel { return panel }
        let view = HistoryPanelView(viewModel: viewModel, imageProvider: imageProvider) { [weak self] item in
            self?.onPaste?(item)
        }
        let hosting = NSHostingView(rootView: view)
        hosting.wantsLayer = true

        let panel = CopyCovePanel(contentRect: NSRect(x: 0, y: 0, width: panelWidth, height: 200),
                                  styleMask: [.nonactivatingPanel, .borderless],
                                  backing: .buffered, defer: false)
        panel.contentView = hosting
        panel.isFloatingPanel = true
        panel.level = .floating
        panel.backgroundColor = .clear
        panel.isOpaque = false
        panel.hasShadow = true
        panel.collectionBehavior = [.canJoinAllSpaces, .fullScreenAuxiliary]
        panel.standardWindowButton(.closeButton)?.isHidden = true
        panel.standardWindowButton(.miniaturizeButton)?.isHidden = true
        panel.standardWindowButton(.zoomButton)?.isHidden = true
        panel.hidesOnDeactivate = false
        panel.isMovable = false

        self.panel = panel
        self.hostingView = hosting
        return panel
    }

    /// 面板中心：屏幕水平居中、垂直上 1/3 处
    private func position(_ panel: CopyCovePanel) {
        guard let screen = NSScreen.main else { return }
        let visible = screen.visibleFrame
        let size = panel.frame.size
        let centerX = visible.midX
        let centerY = visible.maxY - visible.height / 3
        panel.setFrame(NSRect(x: centerX - size.width / 2,
                              y: centerY - size.height / 2,
                              width: size.width,
                              height: size.height), display: true)
    }

    private func popScaleAnimation(on panel: CopyCovePanel) {
        guard let layer = panel.contentView?.layer else { return }
        let scale = CABasicAnimation(keyPath: "transform.scale")
        scale.fromValue = 0.97
        scale.toValue = 1.0
        scale.duration = 0.15
        scale.timingFunction = CAMediaTimingFunction(name: .easeOut)
        layer.add(scale, forKey: "pop")
    }

    private func installKeyMonitorIfNeeded() {
        guard keyMonitor == nil else { return }
        keyMonitor = NSEvent.addLocalMonitorForEvents(matching: .keyDown) { [weak self] event in
            guard let self,
                  let panel = self.panel,
                  panel.isVisible,
                  panel.isKeyWindow else { return event }

            // ⌘; 交给 Carbon 热键处理 toggle，这里吞掉避免蜂鸣
            let flags = event.modifierFlags.intersection(.deviceIndependentFlagsMask)
            if flags.contains(.command), event.charactersIgnoringModifiers == ";" {
                return nil
            }

            switch Int(event.keyCode) {
            case kVK_Escape:
                self.hide()
                return nil
            case kVK_Return:
                self.pasteSelected()
                return nil
            case kVK_DownArrow:
                self.viewModel.moveSelection(1)
                return nil
            case kVK_UpArrow:
                self.viewModel.moveSelection(-1)
                return nil
            default:
                break
            }

            // 数字键 1-9、0 直选
            if flags.isEmpty || flags == .numericPad,
               let char = event.charactersIgnoringModifiers,
               char.count == 1, char >= "0", char <= "9" {
                let index = char == "0" ? 9 : Int(char)! - 1
                if index < self.viewModel.items.count {
                    self.viewModel.selectedIndex = index
                    self.pasteSelected()
                    return nil
                }
            }
            return event
        }
    }

    private func pasteSelected() {
        guard viewModel.selectedIndex < viewModel.items.count else { return }
        onPaste?(viewModel.items[viewModel.selectedIndex])
    }
}
```

- [ ] **Step 4: 编译 + 全量测试**

Run: `swift build && swift test`
Expected: BUILD SUCCEEDED；全部测试通过

- [ ] **Step 5: 写 resources/Info.plist**

```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDevelopmentRegion</key>
    <string>zh_CN</string>
    <key>CFBundleExecutable</key>
    <string>CopyCove</string>
    <key>CFBundleIdentifier</key>
    <string>com.shanyu.CopyCove</string>
    <key>CFBundleInfoDictionaryVersion</key>
    <string>6.0</string>
    <key>CFBundleName</key>
    <string>CopyCove</string>
    <key>CFBundleDisplayName</key>
    <string>CopyCove</string>
    <key>CFBundlePackageType</key>
    <string>APPL</string>
    <key>CFBundleShortVersionString</key>
    <string>0.1.0</string>
    <key>CFBundleVersion</key>
    <string>1</string>
    <key>LSMinimumSystemVersion</key>
    <string>26.0</string>
    <key>LSUIElement</key>
    <true/>
    <key>NSPrincipalClass</key>
    <string>NSApplication</string>
</dict>
</plist>
```

- [ ] **Step 6: 写 scripts/build-app.sh**

```bash
#!/bin/bash
# 把 swift build 产物组装成 CopyCove.app 并 ad-hoc 签名（无需 Xcode）
set -euo pipefail
cd "$(dirname "$0")/.."

echo "==> swift build (release)"
swift build -c release

APP="build/CopyCove.app"
rm -rf "$APP"
mkdir -p "$APP/Contents/MacOS"

cp .build/release/CopyCove "$APP/Contents/MacOS/CopyCove"
cp resources/Info.plist "$APP/Contents/Info.plist"

echo "==> ad-hoc codesign"
codesign --force --sign - "$APP"

echo "==> 完成: $APP"
echo "    运行: open $APP"
```

- [ ] **Step 7: 运行 `chmod +x scripts/build-app.sh && scripts/build-app.sh`**

Expected: 输出 `==> 完成: build/CopyCove.app`，codesign 无报错

- [ ] **Step 8: 写 README.md**

```markdown
# CopyCove

macOS 菜单栏剪贴板历史工具：⌘; 呼出，最近 10 条，选中即粘贴。

## 开发

    swift build && swift test     # 编译 + 单元测试
    scripts/build-app.sh          # 打包 build/CopyCove.app
    open build/CopyCove.app       # 运行

## 首次使用

1. 运行后会请求「辅助功能」权限（用于选中后自动 ⌘V）。
   系统设置 → 隐私与安全性 → 辅助功能 中勾选 CopyCove。
2. 复制一些内容，按 ⌘; 查看历史；↑↓ 选择，回车/数字键直接粘贴，Esc 关闭。

## 设计

见 `docs/superpowers/specs/2026-09-11-copycove-design.md`。
```

- [ ] **Step 9: 手动验收（真实环境，无法自动化）**

1. `open build/CopyCove.app` → 菜单栏出现剪贴板图标，无 Dock 图标
2. 复制一段文本 → ⌘; → 面板居中出现且含该条目，毛玻璃圆角
3. 再复制不同文本 ×11 → 面板最多显示 10 条，最旧被淘汰
4. 复制截图（⌃⇧⌘4）→ 面板出现图片条目及缩略图
5. ↑↓/数字键/回车 → 内容粘贴到当前光标处（需辅助功能权限）
6. Esc / 再按 ⌘; / 点击面板外 → 面板关闭
7. 退出重开 → 历史仍在
8. 菜单栏右键 → 清空历史生效；退出正常

- [ ] **Step 10: Commit**

```bash
git add Sources resources scripts README.md
git commit -m "feat: 玻璃面板 UI、键盘导航、打包脚本与 README（v0.1.0 可用）"
```

---

## Self-Review 结论

- **Spec 覆盖**：轮询监听（Task 5）、去重/淘汰/10 条上限（Task 4）、文本+图片（Task 2/3）、⌘; 热键（Task 8）、居中毛玻璃面板（Task 9）、键盘+数字直选（Task 9）、选中即粘贴+降级（Task 6）、ConcealedType 跳过（Task 5）、超大图片跳过（Task 2/5）、持久化+重启保留（Task 3）、开机启动/清空/退出菜单（Task 8）、无 Dock 图标（Info.plist LSUIElement）——全部有对应 Task。
- **占位符**：Task 6 测试中 `keystrokeSent` 的微调已在正文标注具体写法；无其他 TBD。
- **类型一致性**：`PersistenceStore` 方法名（loadItems/saveItems/saveImage/imageData/deleteImage + loadImage 仅 DiskStore 扩展）、`PasteboardProtocol`、`CapturedContent`、`HistoryStore.add -> Bool` 在各 Task 间一致。
